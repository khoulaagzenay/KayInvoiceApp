@page
@model LoginModel

@{
    ViewData["Title"] = "Log in";
}

<div class="login-container d-flex align-items-center justify-content-center min-vh-100">
    <div class="card shadow-lg border-0 rounded-4 p-4" style="max-width: 420px; width: 100%;">
        <div class="text-center mb-4">
            <img src="~/images/logo.png" alt="Company Logo" class="img-fluid" style="max-width: 200px;">
            <h3 class="text-center text-primary mb-3">@ViewData["Title"]</h3>
            <p class="text-center text-muted mb-4">Log in to your workspace</p>

        </div>
        <form id="account" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger text-center mb-3" role="alert"></div>

            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                <label asp-for="Input.Email" class="form-label">Email</label>
                <span asp-validation-for="Input.Email" class="text-danger small"></span>
            </div>

            <div class="form-floating mb-3">
                <input asp-for="Input.Password" class="form-control" type="password" autocomplete="current-password" aria-required="true" placeholder="Password" />
                <label asp-for="Input.Password" class="form-label">Password</label>
                <span asp-validation-for="Input.Password" class="text-danger small"></span>
            </div>

            <div class="form-check mb-3">
                <input class="form-check-input" asp-for="Input.RememberMe" />
                <label class="form-check-label" asp-for="Input.RememberMe">
                    Remember me
                </label>
            </div>

            <button id="login-submit" type="submit" class="btn btn-primary w-100 py-2 rounded-3">
                Connection
            </button>

            <div class="mt-3 text-center">
                <a id="forgot-password" asp-page="./ForgotPassword" class="text-decoration-none">Forgot Password?</a>
            </div>
        </form>

        <hr class="my-4">

        <div class="text-center">
            <p class="mb-2">You don't have an account ?</p>
            <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl" class="btn btn-outline-primary w-100 rounded-3">Create an account</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

<style>
    body {
        background: white /* linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%) */;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .card {
        background-color: white;
    }

    .form-control:focus {
        border-color: #007bff;
        box-shadow: 0 0 0 0.15rem white; /* rgba(13, 110, 253, 0.25) */;
    }

    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
        font-weight: 500;
        transition: all 0.2s ease-in-out;
    }

        .btn-primary:hover {
            background-color: white;
            border-color: #004a99;
        }
/* 
    .login-container {
        background: linear-gradient(120deg, #e3f2fd 0%, #f8f9fa 100%);
    } */
</style>
